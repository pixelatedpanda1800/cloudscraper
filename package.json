{
  "name": "cloudscraper",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "CloudScraper — M0 deterministic simulation core + debug renderer",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "sim:bench": "tsx scripts/bench.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0"
  }
}
